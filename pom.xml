<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.citeright</groupId>
    <artifactId>citeright</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>CiteRight - Smart Citation Finder</name>
    <description>A smart citation finder that helps researchers find relevant papers to cite</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <javafx.version>21.0.2</javafx.version>
        <gson.version>2.11.0</gson.version>
        <mysql.version>9.1.0</mysql.version>
        <exec.mainClass>com.citeright.CiteRightApp</exec.mainClass>
    </properties>

    <dependencies>
        <!-- JavaFX Controls -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- JavaFX FXML -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- JavaFX Swing integration (SwingFXUtils for PDF rendering) -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- Gson for JSON parsing -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- MySQL Connector (optional — legacy support) -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql.version}</version>
        </dependency>

        <!-- SQLite JDBC — local embedded database (primary storage) -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.45.1.0</version>
        </dependency>

        <!-- Apache PDFBox — PDF rendering and text extraction -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.1</version>
        </dependency>

        <!-- citeproc-java — Pure-Java CSL citation processor -->
        <dependency>
            <groupId>de.undercouch</groupId>
            <artifactId>citeproc-java</artifactId>
            <version>3.5.0</version>
        </dependency>

        <!-- CSL Styles — Bundled citation styles (APA, MLA, IEEE, etc.) -->
        <dependency>
            <groupId>org.citationstyles</groupId>
            <artifactId>styles</artifactId>
            <version>26.2</version>
        </dependency>

        <!-- CSL Locales — Language-specific date/term translations -->
        <dependency>
            <groupId>org.citationstyles</groupId>
            <artifactId>locales</artifactId>
            <version>26.2</version>
        </dependency>

        <!-- JBibTeX — Robust BibTeX parser and writer -->
        <dependency>
            <groupId>org.jbibtex</groupId>
            <artifactId>jbibtex</artifactId>
            <version>1.0.20</version>
        </dependency>

        <!-- Deep Java Library (DJL) for Local Deep Learning Inference -->
        <dependency>
            <groupId>ai.djl</groupId>
            <artifactId>api</artifactId>
            <version>0.28.0</version>
        </dependency>
        <dependency>
            <groupId>ai.djl.huggingface</groupId>
            <artifactId>tokenizers</artifactId>
            <version>0.28.0</version>
        </dependency>
        <dependency>
            <groupId>ai.djl.onnxruntime</groupId>
            <artifactId>onnxruntime-engine</artifactId>
            <version>0.28.0</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                </configuration>
            </plugin>

            <!-- JavaFX Maven Plugin -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>com.citeright.CiteRightApp</mainClass>
                </configuration>
            </plugin>

            <!-- Maven Exec Plugin (for running) -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                </configuration>
            </plugin>

            <!-- Maven Shade Plugin (for packaging into a single executable JAR) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>standalone</shadedClassifierName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.citeright.Launcher</mainClass>
                                </transformer>
                                <!-- Merge META-INF/services files so both SQLite and MySQL JDBC drivers register -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <!-- Remove conflicting module-info files from dependencies -->
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>module-info.class</exclude>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <!--
        ╔══════════════════════════════════════════════════════════════════╗
        ║  PORTABLE BUILD PROFILE                                         ║
        ║                                                                  ║
        ║  Produces a self-contained app that runs on any Windows PC       ║
        ║  without requiring Java to be installed.                         ║
        ║                                                                  ║
        ║  Usage:  build.bat  (or run manually):                          ║
        ║    mvn package -P portable                                      ║
        ║                                                                  ║
        ║  Output: target/CiteRight-portable/CiteRight.exe                ║
        ╚══════════════════════════════════════════════════════════════════╝
    -->
    <profiles>
        <profile>
            <id>portable</id>
            <build>
                <plugins>
                    <!-- Copy standalone JAR to a clean directory to prevent jpackage from recursively packaging target/CiteRight-portable -->
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>copy-jar-for-jpackage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${project.build.finalName}-standalone.jar</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- jpackage: bundle app + trimmed JRE into portable folder -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-portable</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>jpackage</executable>
                                    <arguments>
                                        <!-- Output type: portable folder (no installer, no WiX needed) -->
                                        <argument>--type</argument>
                                        <argument>app-image</argument>

                                        <!-- App identity -->
                                        <argument>--name</argument>
                                        <argument>CiteRight</argument>
                                        <argument>--app-version</argument>
                                        <argument>1.0.0</argument>
                                        <argument>--vendor</argument>
                                        <argument>CiteRight</argument>
                                        <argument>--description</argument>
                                        <argument>Smart Citation Manager</argument>

                                        <!-- Input: folder containing the fat standalone JAR -->
                                        <argument>--input</argument>
                                        <argument>${project.build.directory}/jpackage-input</argument>

                                        <!-- Entry point -->
                                        <argument>--main-jar</argument>
                                        <argument>${project.build.finalName}-standalone.jar</argument>
                                        <argument>--main-class</argument>
                                        <argument>com.citeright.Launcher</argument>

                                        <!-- Output destination -->
                                        <argument>--dest</argument>
                                        <argument>${project.build.directory}/CiteRight-portable</argument>

                                        <!-- JVM options passed to the launched app -->
                                        <argument>--java-options</argument>
                                        <argument>-Dfile.encoding=UTF-8</argument>
                                        <argument>--java-options</argument>
                                        <argument>-Xmx512m</argument>
                                        <argument>--java-options</argument>
                                        <argument>--add-opens=java.base/java.lang=ALL-UNNAMED</argument>
                                        <argument>--java-options</argument>
                                        <argument>--add-opens=java.base/java.lang.reflect=ALL-UNNAMED</argument>
                                        <argument>--java-options</argument>
                                        <argument>--add-opens=java.base/java.util=ALL-UNNAMED</argument>
                                        <argument>--java-options</argument>
                                        <argument>--add-opens=java.base/java.io=ALL-UNNAMED</argument>

                                        <!-- App icon -->
                                        <argument>--icon</argument>
                                        <argument>${project.basedir}/src/main/resources/icon.ico</argument>

                                        <!-- Ensure key modules are included in the bundled JRE -->
                                        <argument>--add-modules</argument>
                                        <argument>java.base,java.desktop,java.logging,java.sql,java.net.http,java.xml,java.naming,jdk.crypto.ec,jdk.unsupported</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
